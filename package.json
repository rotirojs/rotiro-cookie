{
  "name": "@rotirojs/rotiro-cookie",
  "version": "1.0.1",
  "description": "Rotiro Cookie Middleware",
  "author": "Alex Everitt",
  "scripts": {
    "test": "jest --detectOpenHandles",
    "test:ci": "jest --ci --coverage --detectOpenHandles",
    "clear-build": "rm -rf lib/*",
    "build": "rm -rf lib/* && tsc",
    "prepublishOnly": "rm -rf lib/* && tsc",
    "lint": "tslint -c tslint.json 'src/**/*.ts'",
    "lint-fix": "tslint -c tslint.json 'src/**/*.ts' --fix"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rotirojs/rotiro-cookie.git"
  },
  "license": "MIT",
  "types": "lib/index.d.ts",
  "main": "lib/index.js",
  "devDependencies": {
    "@types/jest": "^24.9.1",
    "@types/node": "^14.14.5",
    "jest": "^26.6.3",
    "prettier": "^1.19.1",
    "ts-jest": "^26.4.4",
    "tslint": "^5.20.0",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.0.5"
  },
  "dependencies": {
    "@types/cookie": "^0.4.0",
    "cookie": "^0.4.1",
    "rotiro": "^1.x.x"
  },
  "bugs": {
    "url": "https://github.com/rotirojs/rotiro-cookie/issues"
  },
  "homepage": "https://github.com/rotirojs/rotiro-cookie#readme",
  "directories": {
    "lib": "lib"
  }
}
